# Commit Conventions — CONSOLIDATED

Commit message format, types, scopes, and AB# requirements are defined in `rules/global-standards.md` §Commit Message Format.

Enforcement: `hooks/commit-msg.sh` (hard block on format violations).
