# Shared stage includes

| File | Purpose |
|------|---------|
| `rpi-serialization-baseline.md` | Common Research / Plan / Implement contract for stack variants (links to `rules/rpi-workflow.md`). |

**Convention:** Implementation-stage variants (`08-implementation/variants/*.md`) should link this file under **RPI Rules** and list only **stack-specific** research/plan/implement bullets below it.
